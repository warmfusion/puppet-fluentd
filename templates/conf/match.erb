
# !!! generated by puppet !!!
# Matcher for '<%= @name %>''

<match <%= @pattern.kind_of?(Array) ? @pattern.join(' ') : @pattern -%> >
   type <%= @type %>
<%
  @type_config.each do |option, param| -%>
   <%= option -%> <%= param %>
<% end %>
</match>
